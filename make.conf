# These settings were set by the catalyst build script that automatically
# built this stage.
# Please consult /usr/share/portage/config/make.conf.example for a more
# detailed example.
COMMON_FLAGS="-march=native -O2 -pipe"
CFLAGS="${COMMON_FLAGS}"
CXXFLAGS="${COMMON_FLAGS}"
FCFLAGS="${COMMON_FLAGS}"
FFLAGS="${COMMON_FLAGS}"

MAKEOPTS="-j8"

VIDEO_CARDS="amdgpu radeonsi"
ACCEPT_LICENSE="*"
USE="pulseaudio screencast wayland \
	-Xaw3d -cdda -cddb -cdinstall -dvb \
	-dvd -policykit -suid -systemd -vcd \
	-accessibilty -altivec -aqua -bluetooth -cdr \
	-clamav -connman -coreaudio -dvdr -elogind \
	-emacs -geoip -geolocation -gnome -gnome-keyring \
	-ios -ipod -kde -neon -networkmanager -nvenc \
	-plasma -samba -smartcard -telemetry -xemacs"

L10N="en_US"

# NOTE: This stage was built with the bindist Use flag enabled
PORTDIR="/var/db/repos/gentoo"
DISTDIR="/var/cache/distfiles"
PKGDIR="/var/cache/binpkgs"

# This sets the language of build output to English.
# Please keep this setting intact when reporting bugs.
LC_MESSAGES=C
GRUB_PLATFORMS="efi-64"
GENTOO_MIRRORS="http://mirror.init7.net/gentoo/ http://mirror.netcologne.de/gentoo/ http://mirror.eu.oneandone.net/linux/distributions/gentoo/gentoo/ http://mirror.leaseweb.com/gentoo/ http://mirrors.soeasyto.com/distfiles.gentoo.org/ http://ftp-stud.hs-esslingen.de/pub/Mirrors/gentoo/ http://linux.rz.ruhr-uni-bochum.de/download/gentoo-mirror/ http://ftp.fau.de/gentoo http://ftp.free.fr/mirrors/ftp.gentoo.org/ https://mirrors.soeasyto.com/distfiles.gentoo.org/"
